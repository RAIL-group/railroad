# Python-generated files
__pycache__/
*.py[oc]
build/
# Build artifacts from pybind11
/packages/railroad/src/railroad/*.so
/packages/railroad/src/railroad/_bindings.pyi
dist/
wheels/
*.egg-info
.pytest_cache/

# Virtual environments
.venv

# MacOS-specific
*.DS_Store

# Resources
resources/
# Anchored: this is the repo-root output directory (test_logs, replay,
# *_episodes). Unanchored it also matched any nested directory named `data`,
# which silently swallowed packages/railroad/tests/pddl_converter/data --
# fixtures the tests need, so they passed locally and failed on CI.
/data/
# Every uv.lock, not just the root one: running uv from inside a package
# directory leaves a stray lockfile there, and we don't track any of them.
uv.lock


# Logging
mlruns
mlflow.db
.worktrees/
.benchmark_cache/

# Local scratch trees, tracked on no branch. Anchored, like /data/ above.
# This also keeps `ty check` clean: ty respects .gitignore, and this tree calls
# a tutorial API that is not in the source, so unignored it reports 261 errors.
/railroad-tutorial/
